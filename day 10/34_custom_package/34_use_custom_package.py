# Using the custom package

from custom_package import geometry  # ✅ works when run directly

print("Square area:", geometry.area_square(5))          # Output: 25
print("Rectangle area:", geometry.area_rect(5, 4)) # Output: 20
